# These are all default values I can use for variables
# The only one we're really using is the image.tag though
replicaCount: 1

image:
  repository: pyrosh0ck/miniprojects-devops
  pullPolicy: IfNotPresent
  tag: "

ingress:
  enabled: false
  className: ""
  annotations: {}
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: ImplementationSpecific
